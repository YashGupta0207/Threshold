# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment / secrets — never commit these
.env
.env.*

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Gmail API secrets — never commit
credentials.json
credentials.json.json
token.json

# PyInstaller & Build junk — NEVER COMMIT THE MASSIVE .EXE
build/
dist/
*.exe
*.spec